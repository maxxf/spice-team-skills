---
description: Show every retention workflow available in this plugin and how to trigger it
---

Trigger the `retention-help` skill to render the full retention operating system index for the user.

Do not write any preamble. Invoke the skill directly.

After the skill returns, end the response with a single sentence inviting the next action:
"What do you want to run first?"
